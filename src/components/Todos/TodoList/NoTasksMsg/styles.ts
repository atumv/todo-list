import styled from "styled-components";
import { variables as v } from "@styles/variables";

export const StyledNoTasksMsg = styled.p`
  color: ${v.textColor};
  font-size: 1rem;
  margin: 0.3rem 0;
  text-align: center;
`;
